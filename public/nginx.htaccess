location ~* (runtime|application)/{
	return 403;
}
location / {
    #rewrite ^/(abc)$ /getc.php?q=$1 last;
    #rewrite ^/(article.*)$ /index/article.html??s=$1 last;
	if (!-e $request_filename){
		#rewrite  ^(.*)$  /index.php?s=$1  last;
		rewrite ^/(.*)$ /index.php?s=/$1 last;
	}
	# 移除 /index/ 前缀
    #if ($request_uri ~ "^/index/(.*)") {
    #    set $new_uri $1;
    #    rewrite ^/index/(.*)$ /$new_uri last;
    #}
}
